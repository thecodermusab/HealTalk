---
phase: 07-realtime-messaging
plan: 01
type: execute
depends_on:
  - phase: 06-video-consultations
    plan: 05
files_modified: []
---

<objective>
Set up Socket.io server and client connection with Next.js.

Purpose: Provide real-time transport for chat messages.

Output: Socket.io server route and client helper for connecting.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

**Requirements:**
- Socket.io v4 server
- Authenticated connections via NextAuth session
- Works in App Router
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install socket.io dependencies</name>
  <files>package.json, package-lock.json</files>
  <action>
Install `socket.io` and `socket.io-client`.
  </action>
  <verify>
Dependencies added.
  </verify>
  <done>
- Socket.io packages installed
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Socket.io server route</name>
  <files>src/pages/api/socket.ts (new)</files>
  <action>
Create a Socket.io server using Next API route, attach to `res.socket.server`, and authenticate connections using NextAuth `getToken`.
  </action>
  <verify>
Socket server starts once and accepts authenticated connections.
  </verify>
  <done>
- Socket.io server route exists
  </done>
</task>

<task type="auto">
  <name>Task 3: Add client socket helper</name>
  <files>src/lib/socket.ts (new)</files>
  <action>
Create a helper that initializes a singleton `socket.io-client` connection.
  </action>
  <verify>
Client helper exports connect/disconnect.
  </verify>
  <done>
- Socket client helper ready
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Socket.io deps installed
- [ ] Server route created
- [ ] Client helper created
</verification>

<success_criteria>
- Authenticated socket connection available for UI
</success_criteria>

<output>
After completion, create `.planning/phases/07-realtime-messaging/07-01-SUMMARY.md`
</output>
