---
phase: 10-security-hardening
plan: 03
type: execute
depends_on:
  - phase: 10-security-hardening
    plan: 02
files_modified: []
---

<objective>
Configure security headers and CSRF protection for API routes.

Purpose: Mitigate XSS, clickjacking, and request forgery risks.

Output: Security headers in Next.js config and CSRF safeguards on state-changing endpoints.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@next.config.ts
@src/app/api

**Requirements:**
- CSP, HSTS, X-Frame-Options, Referrer-Policy, Permissions-Policy
- CSRF protection for POST/PATCH/DELETE routes
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add security headers</name>
  <files>next.config.ts</files>
  <action>
Add a headers() configuration to apply security headers globally.
  </action>
  <verify>
Headers visible in response.
  </verify>
  <done>
- Security headers configured
  </done>
</task>

<task type="auto">
  <name>Task 2: Add CSRF protection for API routes</name>
  <files>src/lib/csrf.ts (new), src/app/api/**/route.ts</files>
  <action>
Implement CSRF token validation for state-changing routes (POST/PATCH/DELETE). Use double-submit tokens or NextAuth CSRF where applicable.
  </action>
  <verify>
Requests without CSRF token are rejected.
  </verify>
  <done>
- CSRF validation enforced
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Security headers active
- [ ] CSRF checks enforced on mutating routes
</verification>

<success_criteria>
- Responses include security headers
- Mutating requests require CSRF token
</success_criteria>

<output>
After completion, create `.planning/phases/10-security-hardening/10-03-SUMMARY.md`
</output>
