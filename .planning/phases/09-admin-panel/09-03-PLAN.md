---
phase: 09-admin-panel
plan: 03
type: execute
depends_on:
  - phase: 09-admin-panel
    plan: 02
files_modified: []
---

<objective>
Implement content moderation for reviews and blog posts so admins can approve, hide, or remove inappropriate content.

Purpose: Maintain trust and quality by giving admins control over user-generated and editorial content.

Output: Moderation fields in DB, admin API routes, and a moderation UI.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@prisma/schema.prisma
@src/app/api/psychologists/[id]/route.ts
@src/app/api/blog/route.ts
@src/components/dashboard/DashboardLayout.tsx

**Current state:**
- Reviews have no moderation status
- Blog posts already support `published` boolean
- Admin content pages are not implemented

**Requirements:**
- Admin-only moderation
- Approve/hide/reject reviews
- Publish/unpublish blog posts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add review moderation fields</name>
  <files>prisma/schema.prisma</files>
  <action>
Add moderation fields for reviews.

Suggested:
- Add `ReviewStatus` enum (APPROVED, PENDING, REJECTED)
- Add `status` field on Review with default APPROVED
- Optional: add `moderatedAt` and `moderatedById` (admin user) if needed

Run Prisma migration after updating schema.
  </action>
  <verify>
- Migration applies cleanly
- Prisma client updated
  </verify>
  <done>
- Review moderation status available
  </done>
</task>

<task type="auto">
  <name>Task 2: Build admin moderation API routes</name>
  <files>
src/app/api/admin/reviews/route.ts (new)
src/app/api/admin/reviews/[id]/route.ts (new)
src/app/api/admin/blog-posts/route.ts (new)
src/app/api/admin/blog-posts/[id]/route.ts (new)
  </files>
  <action>
Create admin-only endpoints:
- Reviews: list with filters (status, rating), update status, delete
- Blog posts: list all with published flag, toggle publish/unpublish

Ensure public review fetches only approved reviews.
  </action>
  <verify>
- Non-admin requests are rejected
- Admin can update review status and blog published state
  </verify>
  <done>
- Moderation API routes ready
  </done>
</task>

<task type="auto">
  <name>Task 3: Build moderation UI</name>
  <files>
src/app/(dashboard)/admin/dashboard/reviews/page.tsx (new)
src/components/dashboard/DashboardLayout.tsx
  </files>
  <action>
Create an admin moderation page with tabs for Reviews and Blog Posts:
- Reviews: table with rating, comment, author, status + action buttons
- Blog posts: list with publish toggle

Add navigation entry (e.g., "Content" or "Moderation") in the admin sidebar.
  </action>
  <verify>
- Admin can approve/reject reviews
- Admin can publish/unpublish posts
- Navigation shows the moderation page
  </verify>
  <done>
- Moderation UI fully functional
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Review status field and migration created
- [ ] Admin moderation API endpoints work
- [ ] Moderation UI accessible and functional
- [ ] Public review fetch excludes non-approved reviews
</verification>

<success_criteria>
- Admins can moderate reviews and control blog post publishing
</success_criteria>

<output>
After completion, create `.planning/phases/09-admin-panel/09-03-SUMMARY.md`
</output>
